package Java.com.ex;

public class Java0730_2 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
	/*	java.util.Scanner sc=new java.util.Scanner(System.in);
		
		// 사용자 입력은 문자열이다.
		System.out.println("숫자1>>");
		int str1=Integer.parseInt(sc.nextLine());
		
		
		System.out.println("숫자2>>");
		int str2=Integer.parseInt(sc.nextLine());
		System.out.println("입력한 두수의 합은"+(str1+str2)+"입니다.");
		
	
	java.util.Scanner cho1 = new java.util.Scanner(System.in);
	
	System.out.println("첫번째숫자:");

	int str1=Integer.parseInt(cho1.nextLine());

    System.out.println("두번째 숫자:");

    int str2=Integer.parseInt(cho1.nextLine());

    System.out.println("입력한 두 수의 합은" + (str1+str2) +"입니다.");

    //정수

    System.out.println("첫번째숫자:");

    int a=Integer.parseInt(cho1.nextLine());

    System.out.println("두번째 숫자:");

    int b=Integer.parseInt(cho1.nextLine());

    System.out.println("세번째 숫자:");

    int c=Integer.parseInt(cho1.nextLine());

    int sum=a+b+c;

    System.out.println("입력한 세 수의 합은" +sum+"입니다.");


   // 실수
System.out.println("첫번째숫자:");

double a1=Double.parseDouble(cho1.nextLine());

System.out.println("두번째 숫자:");

double b1=Double.parseDouble(cho1.nextLine());

System.out.println("세번째 숫자:");

double c1=Double.parseDouble(cho1.nextLine());

double result=a1+b1+c1;

System.out.println("입력한 세 수의 합은" +result+"입니다.");

// 문자열

System.out.println("문자열입력:");

String a2=cho1.nextLine();

System.out.println("입력한 문자는 " +a2+"입니다.");

//Boolean

System.out.println("true or false>>");

boolean flag=Boolean.parseBoolean(cho1.nextLine());

System.out.println("입력한 값은 :" +flag+"입니다.");

//true, false 출력기 true TRUE만 true로 출력이 되고 나머지 전부를 false로 출력한다

	
	
		
	java.util.Scanner cho2 = new java.util.Scanner(System.in);
	
	System.out.println("첫번째 실수 입력>>");
	double i1=Double.parseDouble(cho2.nextLine());
	System.out.println("입력된 실수:" + i1);
	
	System.out.println("두번째 실수 입력>>");
	double i2=Double.parseDouble(cho2.nextLine());
	System.out.println("입력된 실수:" + i2);
	
	System.out.println("두번째 실수 입력>>");
	double i3=Double.parseDouble(cho2.nextLine());
	System.out.println("입력된 실수:" + i3);
	
	double totalsum= i1 + i2 + i3;
	System.out.println("세실수의 합은 " + totalsum + "입니다." );
		
		*/
	}

}
